 <div class="row justify-content-center mt-5">
    <div class="col-6">
      <%= simple_form_for [@cocktail, @dose] do |f| %>
        <%= f.input :description, placeholder: "Put a cocktail name" %>
        <%= f.association :ingredient %>
        <%= f.submit "Add", class: "btn btn-primary" %>
      <% end %>|
    </div>
  </div>
